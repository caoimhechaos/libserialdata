package serialdata;

message SerialDataBlockHead
{
	// The length of the current block which needs to be seeked forward to
	// find the successor.
	required uint32 block_length = 1;

	// Optional CRC32 checksum.
	optional uint32 checksum = 2;
}
